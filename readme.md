#### Put files in the global configuration folder: `~/.config/opencode`

##### For vanilla OpenCode:
- Move everything from `/opencode` folder.
- Replace if needed.

##### For OpenCode + Oh-My-OpenCode:
- Install `Oh-My-OpenCode` first.
- Move everything from `/opencode+oh-my-opencode` folder.
- Replace if needed.

Restart `opencode`.

Have fun!